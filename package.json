{
  "scripts": {
    "postinstall": "mkdir -p api/analyze && cp api/analyze.js api/analyze/index.js"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cheerio": "^1.0.0",
    "express-rate-limit": "^6.7.0"
  }
}